# Where we are — end of 2026-08-03

Pick-up notes. Working tree clean, everything below is pushed to `main` (`e90590e`).
Previous note: `docs/archive/handover-2026-07-31.md`.

## The one-line summary

**Payments are done — the whole path is proven, sandbox purchase through to access granted.**
That was the last technical unknown. What is left before Stage 1 is content, and the single
biggest lever is not ours: **46 sign questions are sitting in the admin area waiting for
Louwrens**, and approving them clears the last section gate.

## Live numbers (measured 2026-08-03, not remembered)

| Section | Approved | Draft | Stage 1 target | Gap |
|---|---|---|---|---|
| Rules | **120** | 0 | 120 | met |
| Signs | **73** | **46** | 112 | **39 — but the 46 drafts close it** |
| Controls | **37** | 0 | 24 | met (13 over) |
| **Total** | **230** | 46 | 256 | 26 |

Every one of the 46 drafts is a signs question. Approve them and signs reads **119/112** —
all three sections met, and the Stage 1 content floor is real for the first time.

| | |
|---|---|
| `road_signs` | 381 rows, **352 served** (was 375 — 23 withdrawn today) |
| Question citations | 188/230 approved carry a `source_citation`; **42 do not** |
| `approved_by` | **3/230** — the hard-block added today only binds new approvals |
| `entitlements` | 4 rows — the newest is the first ever `source: payfast` |

## Start here tomorrow

**a) Chase the 46 approvals (K53-39).** Nothing we can build beats this. It is the whole
remaining signs gap, already written and sourced, and it costs Louwrens about an hour. The
end-of-day report (K53-42) puts the ask in front of him in those terms.

**b) Rework the road-marking artwork — John's call, 2026-08-03.** The 16 markings are **live and
served**, and the artwork is functional but ugly: every one renders as a grey **oval** vignette
lifted from the DoT chart, which is a different visual language from the 336 rectangular sign
plates around it. Several are *double* ovals (RM2, RM5, RM12, RM13 — the 24-hour / selective-
periods pairs) and crop badly at thumbnail size; some carry chart furniture that is not part of
the marking (green verge, direction arrowheads, a `STOP` legend on RTM1). Redraw as a consistent
rectangular road-surface tile, one aspect ratio across all 16, no chart furniture, two-case
markings split into a labelled pair. Accuracy spec unchanged: `docs/verification-worklist.md`
rows 71–84. Full note on **K53-37**. Not a launch blocker — they teach correctly — but it is the
most visibly unfinished surface in the app.

**c) Settle the 42 uncited questions.** They are approved and uncitable as they stand. The admin
editor now refuses to approve without a citation, so this is the last cohort that can exist in
that state. Either cite them or drop them back to draft.

**d) The claims audit.** Still a launch gate, still the smallest job of the four. Today moved the
sign-verification claim much closer to true but did not finish it (see the trap below).

**e) Readiness answered 0 questions on production.** Found by the e2e run against
`k53coach.co.za`; works locally. Most likely the flow racing hydration on a slower page rather
than a product fault — **but that is a guess, not a finding.** Reproduce before believing either
reading: `node scripts/e2e/flow.mjs readiness --base https://k53coach.co.za --headed`.

## What changed today

- **PayFast ITN verified end to end.** Real sandbox purchase; all four gates in `verifyItn`
  passed on live traffic; first `source: payfast` entitlement granted for 90 days; replay of the
  same `(source, reference)` rejected by `entitlements_payment_reference_uniq` (23505); paid user
  reaches `/en/mock`; second checkout correctly 409s. Production checkout stays closed (503) by
  the Stage 1 gate — unchanged, deliberately.
- **23 signs withdrawn.** 13 blank plates (border drawn, legend missing — the sign minus its
  meaning), 7 de-restriction signs with the wrong marker, 3 that do not exist in SA.
- **93 Brave-sourced signs chart-verified** by locating each plate on the chart via its caption
  bounding box and rendering it beside ours. 54 corrected from a false `not_in_chart`.
- **48 questions written** (41 signs, 7 temporary-signs) plus the RR30 temporary-signs lesson.
- **Free readiness test is now genuinely 5 questions**, weighted to the real exam mix and
  rotating. It had been 15 against a landing page promising 5.
- **Citation hard-block** in the admin question editor; `approved_by`/`verified_at` now recorded.
- **e2e flow runner** (`scripts/e2e/flow.mjs`) — landing, readiness, paywall, learn, mock,
  checkout, entitled, af. Runs in about a minute.
- **Constraint 10** added to `CLAUDE.md`: learner prose teaches, it is not a technical spec.

## Things that will bite whoever picks this up

**The chart flags now contradict each other on 75 served signs.** Today's repair wrote the
top-level columns `in_official_chart` / `alignment`, but each row *also* carries
`verification.foundOnChart` in its JSON, and that was left alone. So R301-P reads
`in_official_chart: true, alignment: "aligned"` at the top level and `foundOnChart: false`
inside `verification`. Counting served signs "not on the chart" gives **3** (`IN16`, `IN17`,
`R242`) by column and **78** by JSON. Decide which is canonical and reconcile before quoting
either — this is exactly the kind of parallel record that produced the original provenance lie.

**`review_status='approved'` still does not mean a human approved it.** 3 of 230. The hard-block
added today binds *new* approvals only; the existing bank is unchanged and still AI-swept. The
Stage 1 count rests on it.

**Running the ITN again needs a public URL, and neither obvious option works.** Production is
correctly closed (503 `checkout_closed`); Vercel previews sit behind SSO
(`ssoProtection: all_except_custom_domains`) *and* carry neither `PAYFAST_SANDBOX_*` nor any
Supabase keys, so a preview would sign with PayFast's published test pair against no database.
Tunnel the dev server instead — recipe in `CLAUDE.md`. **Kill the tunnel afterwards**; while it
is up the dev server is on the public internet.

**The e2e buyer holds a live entitlement now**, so a repeat payment run stops at 409. Clear
entitlement `ce1b7f96` to test payment again.

**A blank plate is a different defect from a wrong one.** The `-600` signs drew the *wrong*
marker; the 13 pulled today drew *nothing* inside a correct border. Rendered colour depth
separates them (blank ~250–410 unique colours, real 1300+) but the heuristic alone produces
false positives — W405/W406, W401/W402 and IN7 are legitimately simple and do match. Confirm
every candidate by eye.

**Louwrens' Linear account is `louwrens@willsdatabase.com`**, not the `louwrensluyt@gmail.com`
recorded in `CLAUDE.md`. Assign to the account that exists.

## Open decisions for John

- **a)** Whether to post the payment verification to Linear — **K53-32 currently understates
  where payments are**, and Louwrens may still think they are the risk. They are not; content is.
- **b)** Whether `EndofDay` and the pre-existing `Daily Update` label should be collapsed into
  one. Two labels for one concept as of today.
- **c)** Whether the 42 uncited questions get cited or dropped back to draft.
